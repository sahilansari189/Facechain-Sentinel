# --- core ---
python-dotenv==1.0.1
requests==2.32.3
numpy>=1.24,<2.3
Pillow>=10.4.0

# --- face detection / embeddings (InsightFace + ArcFace via ONNXRuntime) ---
# 0.7.3 is source-only on PyPI and requires MSVC to compile on Windows.
# 1.0.1 ships an official pure-Python wheel; FaceAnalysis remains compatible.
insightface==1.0.1
onnxruntime>=1.19.2
opencv-python>=4.10.0.84

# --- html / metadata extraction ---
beautifulsoup4==4.12.3
lxml>=5.3.0

# --- blockchain ---
web3>=7.4.0,<8
py-solc-x==2.0.3

# --- tests ---
pytest==8.3.3
