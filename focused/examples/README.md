# examples/

Put a face image here (e.g. `input.jpg`) and run:

```bash
python src/main.py --image examples/input.webp
```

Use only images you own or are authorised to process. No sample face is committed on
purpose: the pipeline is identity-agnostic and must work with any input image without
source changes.
